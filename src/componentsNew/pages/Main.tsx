import React from 'react';

export default function Main() {
  return <div></div>;
}

// 다솜 님,
// 여기는 MainContainer 에서 내려받을 상태가 딱히 없는 것 같습니다.
// MainContainer에서는 return 안의 내용만 줄 것 같은데, 그러면 MainContainer 부분이 껍데기만 남게 될 것 같네요
